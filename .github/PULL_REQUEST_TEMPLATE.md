## Summary

What changes, and why.

## Checklist

- [ ] Tests cover the change (or a note on why not)
- [ ] `uv run ruff check .` and `uv run ruff format .` pass
- [ ] `uv run ty check` is clean
- [ ] Commit messages follow the conventional style (`feat:`, `fix:`, `test:`, `chore:`, `docs:`)
